# 打包所需的依赖
# 使用方法: pip install -r requirements_build.txt

# 核心依赖
PySide6>=6.5.0
requests>=2.31.0
Pillow>=10.0.0

# 打包工具
pyinstaller>=6.0.0

# 可选：UPX 压缩（需要单独下载 UPX）
# 下载地址: https://github.com/upx/upx/releases
